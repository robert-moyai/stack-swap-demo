# Server-only Firecrawl key. Do not prefix with VITE_ — it must never reach the browser.
# Copy this file to .env and restart the Vite dev server.
FIRECRAWL_API_KEY=fc-YOUR_API_KEY
OPENAI_API_KEY=sk-YOUR_API_KEY
# Optional; defaults to gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini
